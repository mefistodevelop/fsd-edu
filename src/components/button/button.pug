//- 
    options.style(string) - определяет вид кнопки (default/bordered/ghost/big)
    options.text(string) - текст кнопки
    options.type(string) - тип кнопки (submit/button/reset)
    options.href(string) - параметр принимает ссылку. При его наличии создается ссылка, а не кнопка

mixin button(options)
  - const className = options.style ? "button button_" + options.style : ""

  if (options.href)
    a(class= className href= options.href)= options.text
  
  else
    button(class= className type= options.type)= options.text